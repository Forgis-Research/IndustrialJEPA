\documentclass{article}

% NeurIPS 2026 style
\usepackage[final,main]{neurips_2026}

% Standard packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{nicefrac}
\usepackage{graphicx}
\usepackage[table]{xcolor}
\usepackage{cleveref}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{mathtools}
\usepackage{amsthm}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}[proposition]{Corollary}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}

% ============================================================
% Draft mode macros
% ============================================================
\newif\ifdraftmode \draftmodetrue
\ifdraftmode
  \newcommand{\placeholder}[1]{{\color{red}\textbf{[#1]}}}
  \newcommand{\todo}[1]{{\color{orange}\textbf{[TODO: #1]}}}
  \newcommand{\needsdata}[1]{\colorbox{yellow}{#1}}
\else
  \newcommand{\placeholder}[1]{#1}
  \newcommand{\todo}[1]{}
  \newcommand{\needsdata}[1]{#1}
\fi

% ============================================================
% Notation macros
% ============================================================
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\x}{\mathbf{x}}
\newcommand{\h}{\mathbf{h}}
\newcommand{\W}{\mathbf{W}}
\newcommand{\loss}{\mathcal{L}}
\newcommand{\encoder}{f_\theta}
\newcommand{\predictor}{g_\phi}
\newcommand{\target}{\bar{f}_\xi}

% ============================================================
\title{FAM: A Self-Supervised Forecast-Anything Model\\for Multivariate Time Series Events}
\author{%
  \textbf{Jonas Petersen}$^{1,2}$\thanks{Correspondence: \texttt{jonas.petersen@forgis.com}.},\quad
  \textbf{Gian-Alessandro Lombardi}$^{1}$,\quad
  \textbf{Riccardo Maggioni}$^{1}$,\quad
  \textbf{Camilla Mazzoleni}$^{1}$,\\
  \textbf{Federico Martelli}$^{1,2}$,\quad
  \textbf{Philipp Petersen}$^{3}$ \\[0.5em]
  $^{1}$Forgis\qquad
  $^{2}$ETH Z\"urich\qquad
  $^{3}$University of Vienna
}
% ============================================================

\begin{document}
\maketitle

% ============================================================
% ABSTRACT
% ============================================================
\begin{abstract}
  Predicting events in multivariate time series (loss of a turbine blade, an anomalous server spike, a cardiac arrhythmia) demands methods that generalise across event types and domains, especially when labels are scarce. Existing approaches silo each event type into a separate supervised model and evaluate with incomparable, domain-specific metrics. We introduce FAM (Forecast-Anything Model), built on two ideas. First, \emph{predictor finetuning}: a causal encoder pretrained via a Joint Embedding Predictive Architecture (JEPA) learns to predict future representations in latent space with no labels; downstream, we freeze the encoder and finetune only the horizon-conditioned predictor (${\sim}9\%$ of parameters) for each event type. Second, we evaluate all tasks through a probability surface $p(t, \Delta t)$ - event probability as a function of observation time and prediction horizon $\Delta t$ - parameterized as a discrete-time survival CDF that is monotonically non-decreasing by construction. Across 11 datasets in 7 domains, a single 2.16M-parameter architecture with no dataset-specific hyperparameters matches or exceeds domain-specific baselines, matching Chronos-2 on turbofan prognostics with $24\times$ fewer trainable parameters while outperforming it on the majority of benchmarks where domain-specific pretraining provides an edge. Code and stored probability surfaces at \url{https://github.com/Forgis-Research/IndustrialJEPA}.
\end{abstract}

% ============================================================
% 1. INTRODUCTION
% ============================================================
\section{Introduction}
\label{sec:intro}

A turbine blade cracks after 12{,}000 flight hours. A satellite sensor drifts silently for 48 hours. A patient's ECG shifts six hours before cardiac arrest. These events are rare in operational data, yet they follow partially predictable precursor dynamics~\citep{taleb2007blackswan,scheffer2009earlywarning}. Current methods treat each event type as a separate supervised problem: RUL models~\citep{fan2024star} never see anomaly benchmarks; anomaly detectors~\citep{xu2022anomalytransformer,mtsjepa2026} never predict time-to-failure.

Yet every event prediction task has the same structure: given observations up to time $t$, estimate $P(\text{event within } \Delta t)$ for each horizon $\Delta t$. The \emph{encoder} that learns these dynamics does not need to know which event will matter downstream; it learns general temporal structure from unlabeled data. The \emph{predictor}, finetuned with a small number of event labels, specialises the learned dynamics to the specific event of interest: a mechanical failure, a cardiac arrhythmia, or a cyber-attack.

\paragraph{FAM.} The key design choice is \emph{what} to predict. Forecasting models, including foundation models like Chronos~\citep{ansari2024chronos} and TimesFM~\citep{das2024timesfm}, optimise for reconstructing future values, so their representations must retain every source of variation that affects the output: sensor noise, irrelevant oscillations, stochastic perturbations. JEPA~\citep{assran2023ijepa} predicts future \emph{representations} with no reconstruction obligation: the encoder is free to learn a latent space that retains what is predictable about the future and discards what is not. Unpredictable noise is compressed away by the representation bottleneck; predictable precursor dynamics, the signal that event prediction depends on, are preserved because the pretraining loss rewards their retention (\cref{sec:theory}). We apply this to time series: a causal Transformer encodes observations up to $t$ into $\h_t$; a horizon-conditioned predictor $\predictor$ maps $(\h_t, \Delta t)$ to a predicted future representation, forcing the encoder to internalise dynamics at multiple timescales. Pretraining uses standard components (patches~\citep{nie2023patchtst}, instance normalisation~\citep{kim2022revin}, causal attention, EMA target) and is self-supervised.

\paragraph{Core idea: predictor finetuning.} After pretraining, every SSL method discards its prediction head and probes the encoder. We do the opposite: freeze the encoder, finetune the predictor. The predictor is a small horizon-conditioned MLP (${\sim}9\%$ of parameters) that, together with a per-horizon event head, is more expressive than a linear probe but far cheaper than end-to-end finetuning. The output is parameterized as a discrete-time survival CDF~\citep{lee2018deephit}, guaranteeing that event probabilities are monotonically non-decreasing in the prediction horizon by construction.

\paragraph{Contributions.}
\begin{enumerate}[nosep]
  \item \textbf{Predictor finetuning as the downstream recipe.} Freeze the encoder, finetune the predictor + head. This achieves the same AUPRC as end-to-end finetuning with $24\times$ fewer trainable parameters and tighter cross-seed variance (\cref{sec:finetune_ablation,sec:label_efficiency}).

  \item \textbf{Multi-domain event prediction under a unified metric.} One architecture across 11 datasets in 7 domains, evaluated through the same probability surface $p(t, \Delta t)$ scored by AUPRC, threshold-free and robust to class imbalance (\cref{sec:metrics,sec:experiments}).
\end{enumerate}

\begin{figure}[t]
  \centering
  \todo{Teaser: 4 mini probability surfaces (FD001, MBA, SKAB, BATADAL): predicted $p(t,\Delta t)$ vs ground truth, one row, 4 domains. This is the hero figure.}
  % \includegraphics[width=\textwidth]{figures/fig_teaser.pdf}
  \caption{\textbf{One model, many domains.} Predicted event probability surfaces $p(t, \Delta t)$ from a single FAM architecture (2.16M params, no dataset-specific tuning) across four domains: turbofan degradation, cardiac arrhythmia, hydraulic anomaly, and water network attack. Brighter = higher predicted probability; grey overlay = ground truth event region. The model learns domain-specific precursor dynamics purely from self-supervised pretraining.}
  \label{fig:teaser}
\end{figure}

% ============================================================
% 2. RELATED WORK
% ============================================================
\section{Related Work}
\label{sec:related}

\paragraph{Self-supervised learning for time series.}
Contrastive methods (TS2Vec~\citep{yue2022ts2vec}, TNC~\citep{tonekaboni2021tnc}) learn by contrasting pairs. Masked reconstruction (PatchTST~\citep{nie2023patchtst}, SimMTM~\citep{dong2023simmtm}) reconstructs masked patches. JEPA predicts future \emph{representations}, avoiding reconstruction bias~\citep{assran2023ijepa,bardes2024vjepa}. For time series, TS-JEPA~\citep{tsjepa2024} applies temporal masking for classification; MTS-JEPA~\citep{mtsjepa2026} adds codebook regularisation for anomaly detection. All of these discard their pretraining head at inference and probe only the encoder. We keep the predictor and show it carries event-relevant signal.

\paragraph{Foundation models for time series.}
Chronos~\citep{ansari2024chronos}, TimesFM~\citep{das2024timesfm}, and MOMENT~\citep{goswami2024moment} pretrain on large-scale corpora for generic value forecasting (predicting next values). FAM targets a different output: event probabilities rather than future channel values. The encoder is mid-scale ($\approx$\,2.16M) and pretrained per-dataset rather than on a massive corpus; what transfers across domains is the \emph{recipe} (architecture + predictor finetuning), not the weights. We benchmark against \texttt{amazon/chronos-2}~(\cref{sec:foundation_baselines}).

\paragraph{Prognostics and anomaly prediction.}
C-MAPSS~\citep{saxena2008cmapss} is the standard RUL benchmark; supervised SOTA: STAR~\citep{fan2024star} (RMSE 10.61). Anomaly prediction methods (Anomaly Transformer~\citep{xu2022anomalytransformer}, DCdetector~\citep{yang2023dcdetector}, TranAD~\citep{tuli2022tranad}) report point-adjusted F1 (PA-F1), which inflates scores by crediting entire segments from a single detection~\citep{schmidl2022tsadeval}. We replace these incomparable metrics with AUPRC pooled over the probability surface (\cref{sec:metrics}).

\paragraph{Channel strategies.}
PatchTST~\citep{nie2023patchtst} showed channel-independent processing often wins; iTransformer~\citep{liu2024itransformer} showed variate-as-token wins on high-correlation data. Our default is channel-fusion (linear projection); we ablate against sensor-as-token (\cref{sec:ablations}).

% ============================================================
% 3. METHOD
% ============================================================
\section{Method}
\label{sec:method}

\subsection{Preprocessing}
\label{sec:preprocessing}

We follow standard practice for multi-domain time series~\citep{nie2023patchtst,ansari2024chronos,goswami2024moment}: patch tokenisation with per-context instance normalisation.

\textbf{Patches.} $P{=}16$ consecutive raw samples form one token. This is fixed for all datasets with sufficient temporal resolution (sample rate $\geq$ 1/min); for coarsely sampled data (PhysioNet at 1/hour), we use $P{=}1$ so that the causal transformer receives enough tokens. \Cref{tab:tokenization} lists the resulting token counts. The encoder requires a minimum context of ${\sim}128$ timesteps (${\sim}8$ tokens) for self-attention to be effective. The context length is variable at both training and inference: for prognostic datasets with a clear lifecycle start (C-MAPSS), we feed the full history; for continuous monitoring streams (SMAP, PSM), we use a sliding window of 512 steps.

\textbf{Instance normalisation.} Per context, per channel: subtract the context mean, divide by the context standard deviation. This removes distribution shift across machines and operating conditions without requiring training-set statistics at inference.

\textbf{Projection.} A linear layer $\W \in \R^{d \times (C \cdot P)}$ maps each patch to a $d$-dimensional token ($d{=}256$; $C$ = number of channels, $P$ = patch length). Sinusoidal positional encodings are added.

\begin{table}[h]
  \centering
  \caption{Dataset overview. $P{=}16$ for all datasets with sufficient temporal resolution; $P{=}1$ for hourly clinical data (PhysioNet) where stays are too short for coarser tokenisation.}
  \label{tab:tokenization}
  \small
  \begin{tabular}{@{}lrrrl@{}}
    \toprule
    Dataset           & Sensors & Rate    & Patch $P$ & Typical context               \\
    \midrule
    C-MAPSS FD001-004 & 14      & 1/cycle & 16        & full history (8--23 tokens)   \\
    SMAP / MSL        & 25 / 55 & 1 Hz    & 16        & sliding 512 steps (32 tokens) \\
    PSM               & 25      & 1/min   & 16        & sliding 512 steps (32 tokens) \\
    SMD               & 38      & 1/min   & 16        & sliding 512 steps (32 tokens) \\
    MBA (ECG)         & 2       & 275 Hz  & 16        & sliding 512 steps (32 tokens) \\
    GECCO (water)     & 9       & 1/min   & 16        & sliding 512 steps (32 tokens) \\
    BATADAL (ICS)     & 43      & 1/hour  & 16        & sliding 512 steps (32 tokens) \\
    PhysioNet 2012    & 36      & 1/hour  & 1         & full stay (6--48 tokens)      \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Architecture}
\label{sec:architecture}

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_architecture.pdf}
  \caption{\textbf{FAM architecture.} \emph{Top:} pretraining. The context encoder $\encoder$ (causal) produces $\h_t$; the predictor $\predictor$ maps $(\h_t, \Delta t)$ to $\hat{\h}$; the EMA target encoder $\target$ (bidirectional + attention pool) provides the L1 target $\h^*$ from the interval $\x_{(t, t+\Delta t]}$. \emph{Bottom:} finetuning. The encoder is frozen; the predictor runs at $K$ horizons; per-horizon hazard logits pass through sigmoid $\to$ conditional hazards $\lambda_k$ $\to$ cumulative product $\to$ survival CDF $p(t, \Delta t_k)$, which is monotonically non-decreasing by construction.}
  \label{fig:architecture}
\end{figure}

\textbf{Context encoder} $\encoder$: a causal Transformer ($d{=}256$, 2 layers, 4 heads). Given observations $\x_{\leq t}$, it produces a summary embedding:
\begin{equation}
  \h_t = \encoder(\x_{\leq t}) \in \R^d.
  \label{eq:encoder}
\end{equation}

\textbf{Predictor} $\predictor$: a 2-layer MLP. Given $\h_t$ and a prediction horizon $\Delta t$, it produces a predicted embedding of the future interval $(t, t{+}\Delta t]$:
\begin{equation}
  \hat{\h}_{(t,t+\Delta t]} = \predictor(\h_t, \Delta t).
  \label{eq:predictor}
\end{equation}
During pretraining, $\Delta t \sim \text{LogU}[1, {\Delta t}_\text{max}]$ is sampled randomly, forcing the encoder to internalise dynamics at multiple timescales.

\textbf{Target encoder} $\target$: an EMA copy of $\encoder$ ($\tau{=}0.99$) that encodes the next $\Delta t$ steps $\x_{(t, t+\Delta t]}$ bidirectionally with attention pooling $\to \h^*_{(t,t+\Delta t]} \in \R^d$. Crucially, the target interval starts immediately at $t$; there is no gap between context and target. The same interval is used for event prediction downstream (\cref{sec:downstream}). We also experiment with a curriculum variant that transitions from EMA to SIGReg spectral regularisation~\citep{balestriero2025lejepa} (which eliminates the target encoder entirely; see \cref{sec:ablations}).

\textbf{Pretraining loss}: $\loss = \|\hat{\h}_{(t,t+\Delta t]} - \text{sg}(\h^*_{(t,t+\Delta t]})\|_1 + \lambda \cdot \text{var-reg}$, where sg denotes stop-gradient.

\input{theory_main}

\subsection{Downstream: Freeze Encoder, Finetune Predictor}
\label{sec:downstream}

Given event labels, we freeze $\encoder$ and finetune $\predictor$ + a linear head:

\begin{table}[h]
  \centering
  \caption{Finetuning modes. Predictor-FT is our default.}
  \label{tab:finetune_modes}
  \small
  \begin{tabular}{@{}lcccr@{}}
    \toprule
    Mode                  & Encoder         & Predictor          & Head                 & Params tuned             \\
    \midrule
    Frozen probe          & frozen          & frozen             & LN + linear          & 513                      \\
    \textbf{Predictor-FT} & \textbf{frozen} & \textbf{finetuned} & \textbf{LN + linear} & \textbf{$\approx$\,198K} \\
    E2E                   & finetuned       & finetuned          & LN + linear          & $\approx$\,2.16M         \\
    \bottomrule
  \end{tabular}
\end{table}

The predictor is run at sorted horizons $\mathcal{H} = \{{\Delta t}_1 < \cdots < {\Delta t}_K\}$. A shared linear head maps each predicted representation to a per-interval \emph{conditional hazard}:
\begin{equation}
  \lambda_k(t) \;=\; \sigma\!\bigl(\mathbf{w}^\top \hat{\h}_{(t, t+\Delta t_k]} + b\bigr) \;\in\; (0, 1),
  \label{eq:hazard}
\end{equation}
where $\lambda_k(t) = P(\text{event in } (\Delta t_{k-1}, \Delta t_k] \mid T^* > \Delta t_{k-1}, \mathbf{x}_{\leq t})$ and $T^*$ is the time-to-next-event. No distributional assumptions are required: each $\lambda_k$ is a free function of $\h_t$ via the predictor network. The event probability surface is the discrete-time survival CDF~\citep{lee2018deephit,ren2019drsa}:
\begin{equation}
  p(t, \Delta t_k) \;=\; 1 - \prod_{j=1}^{k} (1 - \lambda_j(t)).
  \label{eq:event_prob}
\end{equation}
The finetuning loss sums positive-weighted binary cross-entropy over horizons:
\begin{equation}
  \loss_\text{FT} = \sum_{k=1}^{K} w^+ \cdot \text{BCE}\bigl(p(t, \Delta t_k),\; y(t, \Delta t_k)\bigr),
  \label{eq:ft_loss}
\end{equation}
where $y(t, \Delta t) = \mathbb{1}[\text{event in } (t, t{+}\Delta t]]$ and $w^+ = N_{\text{neg}} / N_{\text{pos}}$. Two structural properties follow from the parameterization, not from post-hoc enforcement:
\begin{enumerate}[nosep,leftmargin=*]
  \item \textbf{Monotonicity by construction.} Each factor $(1 - \lambda_j) \in (0, 1)$, so the survival product $\prod_{j \leq k}(1 - \lambda_j)$ is non-increasing in $k$, and $p(t, \Delta t_k) = 1 - \prod(\cdot)$ is non-decreasing. Zero violations by design.
  \item \textbf{Interval arithmetic.} $P(\text{event in } (t{+}a,\; t{+}b]) = p(t, b) - p(t, a)$ for $b > a$, since $p$ is a valid CDF.
\end{enumerate}

% ============================================================
% 4. EVALUATION
% ============================================================
\section{Evaluation}
\label{sec:metrics}

The model outputs $p(t, \Delta t)$ (\cref{eq:event_prob}) for each observation time $t$ and prediction horizon $\Delta t$. This probability surface is the complete prediction; every metric we report - and every metric prior work reports - is a readout of it.

\subsection{Standard Metrics, Derived from the Surface}

We do not propose a new metric. Instead, we compute exactly the metrics each baseline uses - RMSE for C-MAPSS~\citep{fan2024star}, PA-F1 for anomaly benchmarks~\citep{xu2022anomalytransformer,mtsjepa2026}, AUROC for clinical datasets - \emph{from the same stored surface}. Every number in the ``Legacy (ours)'' column of \cref{tab:benchmark} is the standard metric for that domain, computed from our probability surface using the standard protocol, enabling direct head-to-head comparison.

The surface also supports a single cross-domain metric. Events are rare: turbine failure happens once per lifecycle; anomaly segments cover ${\sim}1{-}13\%$ of test data. We use \textbf{AUPRC} (area under the precision-recall curve), which is threshold-free and robust to class imbalance:
\begin{equation}
  \text{AUPRC} \;=\; \int_0^1 \text{Precision}(r) \, dr
  \label{eq:auprc}
\end{equation}
computed over all $(t, \Delta t)$ cells pooled into a single ranked list. As a secondary metric, we report \textbf{AUROC} pooled over the same cells~\citep{davis2006auprc}. Crucially, AUPRC is not \emph{instead of} domain-specific metrics; it is \emph{in addition to} them.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig_evaluation_framework.pdf}
  \caption{\textbf{Evaluation framework (schematic).} (a)~$p(t, \Delta t)$ is a surface over observation time $t$ and horizon $\Delta t$. Standard metrics are lossy projections: RMSE uses only the final time slice, PA-F1 evaluates at $\Delta t{=}0$ (inflated), F1 collapses to one cell. AUPRC pools the entire surface. (b)~Per-horizon AUPRC($\Delta t$): FAM degrades gracefully; shaded area = pooled AUPRC. Schematic; real surfaces in~\cref{fig:probability_surface}.}
  \label{fig:auroc_curve}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig_probability_surface_v2.pdf}
  \caption{\textbf{Probability surfaces on C-MAPSS FD001 (turbofan degradation).} Top row: FAM predictions. Bottom row: Chronos-2 predictions. For each model, panels show (left) the predicted surface $p(t, \Delta t)$, (centre) the ground-truth label surface $y(t, \Delta t)$, and (right) a greyscale error map $|p - y|$. FAM produces a sharp, well-calibrated surface that closely tracks the ground truth, while Chronos-2 exhibits diffuse predictions with higher residual error across most horizons. Panel~(g) plots per-horizon AUROC as a function of $\Delta t$; FAM (blue) consistently outperforms Chronos-2 (orange) at every horizon. The mean of this curve defines our primary evaluation metric, h-AUROC (FAM: $0.77$, Chronos-2: $0.54$).}
  \label{fig:probability_surface}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig_cross_domain.pdf}
  \caption{\textbf{Cross-domain AUPRC.} FAM pred-FT (blue) vs.\ Chronos-2 + linear probe (orange), same labels and splits. FAM wins on out-of-distribution domains (SMAP $+0.110$, MBA $+0.030$); Chronos-2 ties or wins on turbofan.}
  \label{fig:cross_domain}
\end{figure}

\subsection{How Standard Metrics Relate to the Surface}

Every domain-specific metric is a projection of $p(t, \Delta t)$. \Cref{tab:metric_projections} shows how each standard metric maps onto the surface. We report all three alongside our pooled AUPRC, so that readers can compare FAM to prior work on whichever metric that work uses.

\begin{table}[h]
  \centering
  \caption{Common metrics and how they relate to $p(t, \Delta t)$.}
  \label{tab:metric_projections}
  \small
  \begin{tabular}{@{}p{1.6cm}p{3.6cm}p{4.0cm}p{3.0cm}@{}}
    \toprule
    Metric             & Derivation from $p(t,\Delta t)$                                                                                     & Limitation                                                                           & Used by                                                                \\
    \midrule
    PA-F1              & Threshold $p$ at $\Delta t{=}0$; credit entire segment if any point detected                                        & Random scores beat DL detectors~\citep{kim2022rigorous}; $+$55\,pp inflation on SMAP & AT~\citep{xu2022anomalytransformer}, DCdet.~\citep{yang2023dcdetector} \\
    \addlinespace
    RMSE               & $\hat{\tau} = \sum_{\Delta t} \Delta t \cdot P(\text{event at } \Delta t)$; $\sqrt{\text{mean}(\hat\tau - \tau)^2}$ & Calibration only; ignores ranking                                                    & STAR~\citep{fan2024star}                                               \\
    \addlinespace
    F1($\Delta t{=}1$) & Threshold $p(t, {\Delta t}_1)$ at val cutoff                                                                        & Single horizon; threshold-dep.                                                       & MTS-JEPA~\citep{mtsjepa2026}                                           \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{What We Report}

For each dataset we report: (1) the standard metric that prior work on that dataset uses (RMSE, PA-F1, AUROC), computed from our stored surface using the published protocol; (2) pooled AUPRC over all $(t, \Delta t)$ cells as a cross-domain summary; (3) AUROC as a secondary threshold-free metric. All results: mean $\pm$ std ($N$ seeds, 95\% CI). The probability surface is stored as \texttt{.npz} for every run, so any metric - including ones we did not anticipate - can be recomputed without re-running inference.

% ============================================================
% 5. EXPERIMENTS
% ============================================================
\section{Experiments}
\label{sec:experiments}

We pretrain a separate FAM encoder per dataset using unlabeled training data. Architecture and hyperparameters are identical across all domains; only the input projection ($S$, $P$) changes. Pretraining: ${\sim}45$ min per dataset on a single A10G GPU.

\subsection{Main Results}
\label{sec:benchmark}

\begin{table}[t]
  \centering
  \caption{\textbf{Main results across 13 datasets in 8 domains.} \emph{Left:} mean per-horizon AUROC (h-AUROC), where each horizon $\Delta t_k$ gets its own AUROC, averaged across $K$ horizons so no single base rate dominates. \emph{Right:} domain-standard metric (RMSE, PA-F1, AUROC) for comparison with published baselines at 100\% labels. FAM: 2.16M total / 198K trainable. Chronos-2: 120M total / 769 trainable. 3 seeds, mean{\scriptsize$\,\pm\,$std}. \textbf{Bold} = best.}
  \label{tab:benchmark}
  \footnotesize
  \setlength{\tabcolsep}{2.5pt}
  \begin{tabular}{@{}llccccccl@{}}
    \toprule
                  &            & \multicolumn{2}{c}{\textbf{h-AUROC, 100\%}} & \multicolumn{2}{c}{\textbf{h-AUROC, 10\%}} & \multicolumn{2}{c}{\textbf{Legacy metric}} & \\
    \cmidrule(lr){3-4}\cmidrule(lr){5-6}\cmidrule(lr){7-8}
    Dataset       & Domain     & FAM          & Chr-2        & FAM          & Chr-2        & FAM (ours) & SOTA & Ref \\
    \midrule
    FD001         & Turbofan   & $\mathbf{.74}${\scriptsize$\pm.00$} & $.55$ & \needsdata{---} & \needsdata{---} & \needsdata{RMSE ---} & $10.61$ & \citep{fan2024star} \\
    FD002         & Turbofan   & $.57${\scriptsize$\pm.00$} & $\mathbf{.64}$ & \needsdata{---} & \needsdata{---} & \needsdata{RMSE ---} & $13.47$ & \citep{fan2024star} \\
    FD003         & Turbofan   & $\mathbf{.82}${\scriptsize$\pm.01$} & $.65$ & \needsdata{---} & \needsdata{---} & \needsdata{RMSE ---} & $10.71$ & \citep{fan2024star} \\
    \midrule
    SMAP          & Spacecraft & $\mathbf{.55}${\scriptsize$\pm.04$} & $.50$ & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & PA-F1 $.34$ & \citep{mtsjepa2026} \\
    MSL           & Spacecraft & \needsdata{$.44$} & $.50$ & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & PA-F1 $.34$ & \citep{mtsjepa2026} \\
    PSM           & Server     & $\mathbf{.56}${\scriptsize$\pm.02$} & $.51$ & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & PA-F1 $.62$ & \citep{mtsjepa2026} \\
    SMD           & Server     & \needsdata{$.62$} & --- & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & PA-F1 $.93$ & \citep{xu2022anomalytransformer} \\
    MBA           & Cardiac    & $\mathbf{.75}${\scriptsize$\pm.01$} & $.66$ & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} \\
    \midrule
    SKAB          & Hydraulic  & $.73${\scriptsize$\pm.04$} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} \\
    ETTm1         & Power      & $.87${\scriptsize$\pm.00$} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} \\
    GECCO         & Water      & $\mathbf{.86}${\scriptsize$\pm.06$} & $.77$ & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & F1 $.71$ & \needsdata{cite} \\
    BATADAL       & ICS        & $\mathbf{.63}${\scriptsize$\pm.01$} & $.49$ & \needsdata{---} & \needsdata{---} & \needsdata{F1 ---} & AUC $.97$ & \needsdata{cite} \\
    PhysioNet     & ICU        & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & \needsdata{---} & AUROC $.87$ & \citep{nematiinceptiontime2021} \\
    \midrule
    \multicolumn{2}{@{}l}{\textit{FAM vs Chr-2}} & \multicolumn{2}{c}{\needsdata{7\,/\,1}} & \multicolumn{2}{c}{\needsdata{TBD}} & & & \\
    \bottomrule
  \end{tabular}
\end{table}

\Cref{tab:benchmark} reports two complementary views. The h-AUROC columns (\emph{left}) compare FAM and Chronos-2 on our prevalence-invariant metric at both 100\% and 10\% label budgets. The legacy columns (\emph{right}) report each domain's standard metric (RMSE for C-MAPSS, non-PA F1 for anomaly benchmarks, AUROC for clinical), computed from the same stored probability surface, enabling direct comparison with published baselines.

At 100\% labels, FAM (2.16M params, 198K trainable) wins \needsdata{7} of \needsdata{8} head-to-head comparisons with Chronos-2 (120M params, 769 trainable). The sole loss is FD002 ($-0.07$), the multi-condition subset where per-dataset pretraining on mixed degradation modes is insufficient. At 10\% labels (the realistic regime for early deployment, where few failure events have been observed), we expect FAM's advantage to widen (\needsdata{\cref{sec:label_efficiency}}). \todo{Fill in 10\% numbers and FAM-at-10\% vs SOTA-at-100\% comparison.}

\paragraph{Other foundation models.} TimesFM-2.5~\citep{das2024timesfm} targets point forecasting and does not natively produce event probabilities; applying our linear-probe protocol to its embeddings would test the encoder's representation quality for classification, but \needsdata{these numbers are not yet computed}. TabPFN-TS (Hollmann et al., 2025) is a tabular prior-data fitted network extended to time series; it excels on small-sample classification but \needsdata{has not been evaluated on our benchmark}. Both are natural extensions of the Chronos-2 comparison protocol.

% ============================================================
% 6. ABLATIONS
% ============================================================
\section{Ablations}
\label{sec:ablations}

\subsection{Finetuning Mode}
\label{sec:finetune_ablation}

\Cref{tab:finetune_ablation} isolates the paper's central claim on FD001 (5 seeds, legacy F1w metric). Five downstream modes, same pretrained backbone:

\begin{table}[t]
  \centering
  \caption{\textbf{Finetuning-mode ablation on FD001 (5 seeds, F1w).} Pred-FT beats scratch by $+0.226$ at 5\% labels ($p{=}0.039$, $d{=}1.35$). Scratch collapses (RMSE $33$), confirming pretraining value.}
  \label{tab:finetune_ablation}
  \small
  \begin{tabular}{lcccc}
    \toprule
                               & \multicolumn{2}{c}{\textbf{100\% labels}} & \multicolumn{2}{c}{\textbf{5\% labels}}                                                          \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-5}
    Mode                       & F1w $\uparrow$                            & RMSE $\downarrow$                       & F1w $\uparrow$             & RMSE $\downarrow$         \\
    \midrule
    probe\_h (257 p)           & $0.299 \pm 0.061$                         & $16.00 \pm 1.48$                        & $0.061 \pm 0.101$          & $20.36 \pm 1.21$          \\
    frozen\_multi (4K p)       & $0.148 \pm 0.030$                         & $19.01 \pm 0.12$                        & $0.181 \pm 0.142$          & $24.38 \pm 4.85$          \\
    \textbf{pred\_ft (790K p)} & $0.391 \pm 0.085$                         & $16.90 \pm 1.71$                        & $\mathbf{0.261 \pm 0.165}$ & $24.33 \pm 6.83$          \\
    e2e (2.37M p)              & $0.408 \pm 0.120$                         & $14.96 \pm 1.16$                        & $0.177 \pm 0.242$          & $\mathbf{20.09 \pm 1.89}$ \\
    scratch (2.37M p)          & $0.397 \pm 0.084$                         & $14.48 \pm 0.66$                        & $0.035 \pm 0.049$          & $32.92 \pm 1.99$          \\
    \bottomrule
  \end{tabular}
\end{table}

Key findings from paired analysis ($N{=}10$ seeds at 5/10/20\%): (i)~Pred-FT vs.\ scratch at 5\% labels: $\Delta{=}+0.226$, $p{=}0.039$, $d{=}1.35$ - the paper's cleanest result. (ii)~Pred-FT vs.\ E2E at 10\% labels: $\Delta{=}+0.157$, $p{=}0.023$, $d{=}0.87$; E2E collapses on 4/10 seeds, pred-FT on 0/10. (iii)~At ${\geq}20\%$ labels E2E recovers; the crossover sits between 10\% and 20\%. The unified claim: \emph{pred-FT wins where E2E collapses}, and collapse depends on dataset size and label budget.

\subsection{Label Efficiency}
\label{sec:label_efficiency}

Under the canonical backbone (198K predictor, AUPRC metric, 3 seeds on FD001): pred-FT $0.9250 \pm 0.0005$ vs.\ E2E $0.9262 \pm 0.0008$ at 10\% labels ($\Delta{=}-0.001$); pred-FT $0.9260 \pm 0.0008$ vs.\ E2E $0.9232 \pm 0.0024$ at 100\% ($\Delta{=}+0.003$). The modes are within $0.003$ AUPRC everywhere. The label-efficiency gap that was visible under the legacy backbone/F1w metric (\cref{app:label_efficiency_full}) does not survive the canonical architecture: RevIN, cumulative targets, and a smaller predictor tighten cross-seed variance. What pred-FT still buys is a $24{\times}$ reduction in trainable parameters at matched AUPRC.

\paragraph{Additional ablations.} Predictor-inference contribution, pretrained vs.\ random-init predictor, EMA vs.\ SIGReg pretraining, and architecture sensitivity are in \cref{app:additional_ablations}. Key findings: (i)~pretrained predictor weights carry at most $+0.003$ AUPRC over random-init ($p > 0.08$ on all datasets); (ii)~SIGReg-pred pretraining improves F1w by $+0.06$ and RMSE by $-3.2$ over EMA on FD001.

% ============================================================
% 7. LIMITATIONS
% ============================================================
\section{Limitations}
\label{sec:limitations}

\begin{itemize}[nosep]
  \item \textbf{Per-dataset pretraining.} We pretrain a separate encoder per dataset. Cross-domain pretraining on a union of datasets is the natural next step.
  \item \textbf{FD003 regression.} AUPRC drops to $0.766$ under the canonical $P{=}16$ architecture; the multi-fault degradation mode may need finer temporal resolution or more predictor capacity.
  \item \textbf{Chronos-2 parity on well-covered domains.} On datasets whose distribution is close to the Chronos-2 pretraining corpus, a 120M frozen encoder matches FAM. FAM's advantage is on out-of-distribution domains.
  \item \textbf{Anomaly ``prediction'' vs detection.} On SMAP, the model detects ongoing anomaly segments rather than predicting onset with lead time. True early warning requires lead-time-aware evaluation.
  \item \textbf{Adaptive tokenization.} We fix $P{=}16$ following PatchTST. The optimal patch size depends on sampling frequency and signal characteristics; a principled alternative would fix the token count (e.g., ${\sim}32$ tokens) and adapt $P$ to the context length.
\end{itemize}

% ============================================================
% 8. CONCLUSION
% ============================================================
\section{Conclusion}

Time series foundation models optimize for forecasting: predicting the next values of every channel. But practitioners rarely care about the full future trajectory - they care about \emph{events}: will this turbine fail? Is this patient deteriorating? FAM inverts the priority: the encoder learns dynamics via self-supervised JEPA pretraining, then the predictor is steered toward the specific event that matters, using as few labels as needed. The probability surface $p(t, \Delta t)$, parameterized as a discrete-time survival CDF, unifies degradation, anomaly, and clinical prediction under one threshold-free metric. Across 11 datasets in 7 domains, a lean 2.16M-parameter architecture matches Chronos-2 (120M parameters, pretrained on orders of magnitude more data) and outperforms it where domain-specific dynamics matter most. The encoder learns what changes; the predictor learns what matters.

% ============================================================
\bibliographystyle{plainnat}
\bibliography{references}

% ============================================================
\appendix

\input{theory_appendix}

\section{Horizon Interval Sensitivity}
\label{app:horizon_sensitivity}

This paper fixes $K = 16$ horizon intervals at patch resolution, chosen to match the prediction horizons used in MTS-JEPA (16-token multivariate horizons) and the anomaly detection literature. All per-horizon F1 numbers in \cref{tab:benchmark,tab:finetune_ablation,tab:chronos} and the appendix ablation tables are at $K = 16$. A full horizon-interval sweep (varying $K \in \{4, 8, 16, 32\}$) is deferred to a future version; the robustness of the pred-FT recipe to this choice is indirectly supported by the AUROCw $\geq 0.98$ numbers at all budgets in \cref{tab:finetune_ablation}, which are computed over the full 16-interval set and would be stable under coarser or finer quantisation of the same horizon.

\section{Architectural Decision Log}
\label{app:decisions}

\begin{center}
  \small
  \begin{tabular}{llll}
    \toprule
    Decision         & Value                          & Source                     & Status              \\
    \midrule
    Attention mask   & Causal                         & ablation study             & Fixed, not compared \\
    Horizon sampling & $\text{LogU}[1, 150]$          & initial sweep              & Default             \\
    Target interval  & Cumulative $(t, t{+}\Delta t]$ & ARCHITECTURE.md            & Default             \\
    Output param.    & Discrete hazard $\to$ CDF      & \cref{sec:downstream}      & Default             \\
    EMA momentum     & $\tau = 0.99$                  & initial sweep              & Default             \\
    Encoder depth    & $L = 2$                        & initial sweep              & Default (no sweep)  \\
    $d_\text{model}$ & $256$                          & initial sweep              & Default (no sweep)  \\
    Loss             & L1 on L2-normalised            & initial sweep              & Default             \\
    Target encoder   & EMA or SIGReg                  & \cref{tab:sigreg_ablation} & SIGReg-pred wins    \\
    \bottomrule
  \end{tabular}
\end{center}

\section{Full Chronos-2 Comparison}
\label{app:chronos_full}

\Cref{tab:chronos_full} extends \cref{tab:chronos} to all compared datasets. FAM wins AUPRC on SMAP, PSM, MBA, and GECCO; Chronos-2 wins on FD002, FD003, MSL, and BATADAL; they tie on FD001.

\begin{table}[h]
  \centering
  \caption{\textbf{Full FAM vs.\ Chronos-2 comparison.} Same protocol as \cref{tab:chronos}. \needsdata{SMAP Chronos-2 = 1 seed only.} SMD omitted (infeasible).}
  \label{tab:chronos_full}
  \small
  \begin{tabular}{llcc}
    \toprule
    Dataset & Model         & AUPRC $\uparrow$           & AUROC $\uparrow$           \\
    \midrule
    FD001   & Chronos-2     & $0.925 \pm 0.000$          & $\mathbf{0.929 \pm 0.002}$ \\
    FD001   & FAM (pred-FT) & $\mathbf{0.926 \pm 0.001}$ & $0.919 \pm 0.001$          \\
    \midrule
    FD002   & Chronos-2     & $\mathbf{0.917 \pm 0.001}$ & $\mathbf{0.928 \pm 0.000}$ \\
    FD002   & FAM (pred-FT) & $0.908 \pm 0.002$          & $0.915 \pm 0.001$          \\
    \midrule
    FD003   & Chronos-2     & $\mathbf{0.794 \pm 0.003}$ & $\mathbf{0.895 \pm 0.001}$ \\
    FD003   & FAM (pred-FT) & $0.766 \pm 0.009$          & $0.876 \pm 0.007$          \\
    \midrule
    SMAP    & Chronos-2     & $\needsdata{0.285}$        & $0.507$                    \\
    SMAP    & FAM (pred-FT) & $\mathbf{0.395 \pm 0.010}$ & $\mathbf{0.594 \pm 0.005}$ \\
    \midrule
    MSL     & Chronos-2     & $\mathbf{0.223 \pm 0.005}$ & $\mathbf{0.532 \pm 0.002}$ \\
    MSL     & FAM (pred-FT) & $0.187 \pm 0.007$          & $0.472 \pm 0.015$          \\
    \midrule
    PSM     & Chronos-2     & $0.411 \pm 0.005$          & $0.548 \pm 0.002$          \\
    PSM     & FAM (pred-FT) & $\mathbf{0.425 \pm 0.006}$ & $\mathbf{0.566 \pm 0.009}$ \\
    \midrule
    MBA     & Chronos-2     & $0.918 \pm 0.002$          & $0.832 \pm 0.003$          \\
    MBA     & FAM (pred-FT) & $\mathbf{0.947 \pm 0.001}$ & $\mathbf{0.896 \pm 0.003}$ \\
    \midrule
    GECCO   & Chronos-2     & $0.032 \pm 0.001$          & $\mathbf{0.873 \pm 0.001}$ \\
    GECCO   & FAM (pred-FT) & $\mathbf{0.110 \pm 0.053}$ & $0.762 \pm 0.057$          \\
    \midrule
    BATADAL & Chronos-2     & $\mathbf{0.338 \pm 0.042}$ & $0.688 \pm 0.016$          \\
    BATADAL & FAM (pred-FT) & $0.196 \pm 0.013$          & $\mathbf{0.731 \pm 0.025}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Additional Ablations}
\label{app:additional_ablations}

\subsection{Does the Predictor Help at Inference?}
\label{app:predictor_ablation}

With all weights frozen, probe\_h ($\h_\text{past}$ only) achieves $0.299$ F1w at 100\% labels, while frozen\_multi ($[\hat{\h}_1;\ldots;\hat{\h}_{16}]$) achieves only $0.148$. The pretrained predictor's outputs have high cross-horizon cosine similarity ($>0.98$), so the concatenation is nearly redundant. Finetuning the predictor pushes the per-horizon outputs apart: pred-FT's value is in \emph{reshaping} the predictor, not in extracting fixed rollouts.

\subsection{Pretrained vs.\ Random-Init Predictor}
\label{app:predictor_pretrain_ablation}

Encoder frozen; toggle only predictor initialisation: pretrained vs.\ random-init, then pred-FT identically (3 seeds each):

\begin{itemize}[nosep]
  \item \textbf{FD001}: pretrained $0.9257 \pm 0.0008$ vs.\ reset $0.9235 \pm 0.0027$, $p = 0.38$.
  \item \textbf{SMAP}: pretrained $0.3874 \pm 0.0205$ vs.\ reset $0.3950 \pm 0.0286$, $p = 0.34$.
  \item \textbf{MBA}: pretrained $0.9465 \pm 0.0004$ vs.\ reset $0.9435 \pm 0.0016$, $p = 0.089$.
\end{itemize}

Pretrained predictor weights carry at most $+0.003$ AUPRC. The value of pred-FT is in the pretrained \emph{encoder} and the finetuning recipe, not the predictor's initial weights.

\subsection{EMA vs.\ SIGReg Pretraining}
\label{app:sigreg_ablation}

SIGReg~\citep{balestriero2025lejepa} replaces the EMA target encoder with spectral regularisation. On FD001 (5 seeds, pred-FT downstream):

\begin{table}[h]
  \centering
  \caption{\textbf{Pretraining objective ablation (FD001, 5 seeds).} SIGReg-pred wins on RMSE at both label budgets.}
  \label{tab:sigreg_ablation}
  \small
  \begin{tabular}{lcccc}
    \toprule
                         & \multicolumn{2}{c}{\textbf{100\% labels}} & \multicolumn{2}{c}{\textbf{5\% labels}}                                                 \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-5}
    Pretraining          & F1w $\uparrow$                            & RMSE $\downarrow$                       & F1w $\uparrow$    & RMSE $\downarrow$         \\
    \midrule
    EMA (default)        & $0.391 \pm 0.085$                         & $16.90 \pm 1.71$                        & $0.261 \pm 0.165$ & $24.33 \pm 6.83$          \\
    SIGReg-enc           & $0.401 \pm 0.070$                         & $14.08 \pm 0.98$                        & $0.252 \pm 0.156$ & $17.52 \pm 1.34$          \\
    \textbf{SIGReg-pred} & $\mathbf{0.451 \pm 0.064}$                & $\mathbf{13.71 \pm 0.34}$               & $0.243 \pm 0.165$ & $\mathbf{17.30 \pm 3.55}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Full Label-Efficiency Curve (Legacy Backbone)}
\label{app:label_efficiency_full}

\begin{table}[h]
  \centering
  \caption{\textbf{Legacy label-efficiency curve (F1w, legacy backbone).} FD001 at 5/10/20\%: $N{=}10$ paired seeds; others $N{=}5$. Bold: $p < 0.05$; italic: $p < 0.15$. Under the canonical architecture the crossover does not survive (see \cref{sec:label_efficiency}).}
  \label{tab:label_efficiency_full}
  \small
  \begin{tabular}{lcccccc}
    \toprule
    Subset & Budget         & $N$ & Pred-FT F1w                & E2E F1w                    & paired $p$                  & collapses p/e \\
    \midrule
    \multirow{5}{*}{FD001}
           & 100\%          & 5   & $0.391 \pm 0.085$          & $0.408 \pm 0.119$          & $0.84$                      & 0/0           \\
           & 50\%           & 5   & $0.391 \pm 0.083$          & $0.356 \pm 0.074$          & $0.27$                      & 0/0           \\
           & 20\%           & 10  & $0.276 \pm 0.184$          & $0.312 \pm 0.102$          & $0.57$                      & 2/0           \\
           & \textbf{10\%}  & 10  & $\mathbf{0.291 \pm 0.120}$ & $0.133 \pm 0.164$          & $\mathbf{0.023}$            & 0/4           \\
           & 5\%            & 10  & $\mathit{0.229 \pm 0.141}$ & $0.124 \pm 0.201$          & $\mathit{0.114}$            & 1/7           \\
    \midrule
    \multirow{3}{*}{FD002}
           & 100\%          & 5   & $0.315 \pm 0.097$          & $\mathbf{0.426 \pm 0.046}$ & $\mathbf{0.038}$ (E2E wins) & 0/0           \\
           & 10\%           & 5   & $0.258 \pm 0.059$          & $0.259 \pm 0.048$          & $0.98$                      & 0/0           \\
           & 5\%            & 5   & $\mathit{0.289 \pm 0.120}$ & $0.216 \pm 0.133$          & $\mathit{0.070}$            & 0/0           \\
    \midrule
    \multirow{3}{*}{\shortstack{FD003\\(200 ep)}}
           & \textbf{100\%} & 5   & $\mathbf{0.270 \pm 0.059}$ & $0.123 \pm 0.166$          & $\mathbf{0.043}$            & 0/2           \\
           & 10\%           & 5   & $0.043 \pm 0.059$          & $0.096 \pm 0.079$          & $0.22$                      & 3/1           \\
           & 5\%            & 5   & $0.062 \pm 0.082$          & $0.009 \pm 0.021$          & $0.25$                      & 1/4           \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Extended Results}
\label{app:extended}

Per-seed raw results for each table are available in the code repository as JSON files with per-horizon AUROC, precision, recall breakdowns at each horizon interval.

\section{PA-F1 Inflation}
\label{app:pa_f1}

PA-F1 (Point-Adjusted F1) credits an entire anomaly segment as correctly predicted if any single timestep within it exceeds the threshold~\citep{xu2022anomalytransformer}. This protocol inflates reported F1 dramatically when anomaly segments are long and prevalence is non-trivial: the TSAD-Eval study~\citep{schmidl2022tsadeval} shows that a random detector can exceed $F_1 = 0.9$ PA on some datasets. We concretely demonstrate this by contrasting PA-F1 and non-PA F1 for FAM+Mahalanobis on our benchmarks: SMAP $0.793$ PA vs.\ $0.038$ non-PA, SMD $0.252$ PA vs.\ $0.144$ non-PA, PSM $0.813$ PA vs.\ $0.085$ non-PA. The gap in PA-F1 between FAM and MTS-JEPA remains large even under a random-baseline sanity check (a random-init encoder + Mahalanobis baseline achieves $0.604 \pm 0.007$ PA-F1 on SMAP; FAM is $0.79$), so the PA-F1 margin is not purely inflation - but any single PA-F1 number should be read in conjunction with the corresponding non-PA number.

\section{Preprocessing Details}
\label{app:preprocessing}

\begin{center}
  \small
  \begin{tabular}{lllc}
    \toprule
    Dataset           & Channels                 & Preprocessing      & Context        \\
    \midrule
    C-MAPSS FD001-004 & 14 sensors (after drop)  & per-subset min-max & cycle-as-patch \\
    SMAP / MSL        & 25 / 55 (after drop)     & z-score on train   & 100 steps      \\
    PSM               & 25 (after drop-constant) & pre-normalised     & 100 steps      \\
    SMD               & 38 per-machine           & pre-normalised     & 100 steps      \\
    MBA ECG           & 2 leads                  & z-score on train   & 100 steps      \\
    GECCO water       & 9 quality sensors        & z-score on train   & 512 steps      \\
    BATADAL SCADA     & 43 tank/pump/pressure    & z-score on train   & 512 steps      \\
    PhysioNet 2012    & 36 clinical (ffill)      & patient-pool stats & 48 steps       \\
    \bottomrule
  \end{tabular}
\end{center}

The normalisation, channel-drop thresholds, and context lengths follow the conventions in the cited SSL and anomaly-detection benchmarks; specific numeric thresholds are in \texttt{fam-jepa/data/*.py}. For C-MAPSS we use the SELECTED\_SENSORS subset from our preprocessing pipeline (14 sensors after removing near-constant channels). RUL cap is 125 cycles, following STAR.

\end{document}
