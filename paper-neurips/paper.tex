\documentclass{article}

% NeurIPS 2026 style
\usepackage[preprint]{neurips_2026}

% Standard packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{nicefrac}
\usepackage{graphicx}
\usepackage[table]{xcolor}
\usepackage{cleveref}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{microtype}

% ============================================================
% Draft mode macros
% ============================================================
\newif\ifdraftmode \draftmodetrue
\ifdraftmode
  \newcommand{\placeholder}[1]{{\color{red}\textbf{[#1]}}}
  \newcommand{\todo}[1]{{\color{orange}\textbf{[TODO: #1]}}}
\else
  \newcommand{\placeholder}[1]{#1}
  \newcommand{\todo}[1]{}
\fi

% ============================================================
% Notation macros
% ============================================================
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\x}{\mathbf{x}}
\newcommand{\h}{\mathbf{h}}
\newcommand{\W}{\mathbf{W}}
\newcommand{\loss}{\mathcal{L}}
\newcommand{\encoder}{f_\theta}
\newcommand{\predictor}{g_\phi}
\newcommand{\target}{\bar{f}_\xi}

% ============================================================
\title{FAM: A Self-Supervised Forecast-Anything Model\\for Rare Event Prediction}
\author{Anonymous Author(s)}
% ============================================================

\begin{document}
\maketitle

% ============================================================
% ABSTRACT
% ============================================================
\begin{abstract}
Predicting rare events in multivariate time series - loss of a turbine blade, an anomalous server spike, a cardiac arrhythmia - demands methods that generalise across event types and domains. Existing approaches train a separate supervised model for each. We introduce FAM, a 2.37M-parameter causal JEPA encoder that is pretrained per-dataset with no labels, after which we freeze the encoder and finetune only the horizon-conditioned predictor (790K parameters, 33\% of total) together with a small linear head for each downstream event. This \emph{predictor finetuning} is the paper's core idea: the encoder learns domain dynamics; the predictor is steered toward the specific event of interest. We apply the same architecture and pipeline across seven datasets in five domains (turbofan degradation, spacecraft telemetry, server monitoring, cardiac ECG, bearing vibration), evaluating with per-window binary F1 at patch resolution over a 16-window horizon (primary) alongside domain-standard legacy metrics. At 5\% labels on C-MAPSS FD001, pred-FT (F1w $0.26$) beats end-to-end finetuning (F1w $0.18$) and a scratch baseline (F1w $0.04$) despite using $3\times$ fewer trainable parameters. The same pretrained encoder with Mahalanobis scoring yields PA-F1 of $0.81$ (PSM), $0.79$ (SMAP), $0.71$ (MSL) - above MTS-JEPA and under a single architecture. Code and evaluation suite at \placeholder{URL}.
\end{abstract}

% ============================================================
% 1. INTRODUCTION
% ============================================================
\section{Introduction}
\label{sec:intro}

A turbine blade cracks after 12{,}000 flight hours. A satellite sensor drifts silently for 48 hours. A patient's ECG shifts six hours before cardiac arrest. These events are rare in operational data, yet they follow partially predictable precursor dynamics~\citep{taleb2007blackswan,scheffer2009earlywarning}. Current methods treat each event type as a separate supervised problem: RUL models~\citep{fan2024star} never see anomaly benchmarks; anomaly detectors~\citep{xu2022anomalytransformer,mtsjepa2026} never predict time-to-failure.

Yet every event prediction task reduces to two questions: (1)~\emph{will an event occur within horizon $H$?} and (2)~\emph{when?} The model that answers these does not need to know what kind of event it is predicting. It needs to learn the dynamics of normality; the deviation from that normality defines the event.

\paragraph{FAM.} We pretrain a causal Transformer encoder to predict future sensor representations in latent space via a Joint Embedding Predictive Architecture (JEPA)~\citep{assran2023ijepa}. The horizon-conditioned predictor $\predictor$ maps the encoder's summary of the past, $\h_\text{past}$, to a predicted future representation $\hat{\h}_\text{fut}$ at variable horizon $k$, forcing the encoder to internalise dynamics at multiple timescales. Pretraining uses standard components - patches~\citep{nie2023patchtst}, instance normalisation, causal attention, EMA target~\citep{assran2023ijepa}, L1 loss - and is self-supervised.

\paragraph{Core idea: predictor finetuning.} After pretraining, every SSL method discards its prediction head and probes the encoder. We do the opposite: we freeze the encoder and finetune the predictor. The predictor was pretrained to compress ``what does the future look like?'' Finetuning steers it toward ``does the future contain \emph{this} event?'' At $\approx$\,790K predictor parameters plus a $\approx$\,4K linear head, this is more expressive than a linear probe on $\h_\text{past}$ alone (257 parameters) but far cheaper than end-to-end finetuning (2.37M parameters) and more label-efficient at scarce labels. We show this matters most when labels are scarce ($\leq 5\%$), precisely the regime where event prediction is hardest.

\paragraph{Contributions.}
\begin{enumerate}[nosep]
  \item \textbf{Predictor finetuning as the downstream abstraction for JEPA.} Freezing the encoder and finetuning only the predictor + linear head matches or beats full E2E finetuning at $\leq 20\%$ labels and substantially outperforms it at $\leq 5\%$. The predictor is the right level of adaptation for event-specific prediction (\cref{sec:downstream,sec:finetune_ablation}).

  \item \textbf{One architecture, seven datasets, five domains.} The same 2.37M-parameter model, pretrained per-dataset with no labels, matches or exceeds domain-specific supervised models and dedicated SSL methods (MTS-JEPA) on anomaly PA-F1 across C-MAPSS turbofan degradation, SMAP/MSL spacecraft telemetry, PSM server monitoring, SMD server machines, MBA cardiac ECG, and Paderborn bearing vibration (\cref{sec:experiments}). FAM's linear probe is outperformed by Amazon Chronos-T5-tiny (8.4M), but FAM's pred-FT matches Chronos at $3.5\times$ fewer parameters.
\end{enumerate}

% ============================================================
% 2. RELATED WORK
% ============================================================
\section{Related Work}
\label{sec:related}

\paragraph{Self-supervised learning for time series.}
Contrastive methods (TS2Vec~\citep{yue2022ts2vec}, TNC~\citep{tonekaboni2021tnc}) learn by contrasting pairs. Masked reconstruction (PatchTST~\citep{nie2023patchtst}, SimMTM~\citep{dong2023simmtm}) reconstructs masked patches. JEPA predicts future \emph{representations}, avoiding reconstruction bias~\citep{assran2023ijepa,bardes2024vjepa}. For time series, TS-JEPA~\citep{tsjepa2024} applies temporal masking for classification; MTS-JEPA~\citep{mtsjepa2026} adds codebook regularisation for anomaly detection. All of these discard their pretraining head at inference and probe only the encoder. We keep the predictor and show it carries event-relevant signal.

\paragraph{Foundation models for time series.}
Chronos~\citep{ansari2024chronos}, TimesFM~\citep{das2024timesfm}, and MOMENT~\citep{goswami2024moment} pretrain on large-scale corpora for generic forecasting. FAM is complementary: a mid-scale encoder (2.37M) specialised for event prediction, not forecasting. We benchmark against Chronos as the strongest available foundation model (\cref{sec:foundation_baselines}).

\paragraph{Prognostics and anomaly prediction.}
C-MAPSS~\citep{saxena2008cmapss} is the standard RUL benchmark; supervised SOTA: STAR~\citep{fan2024star} (RMSE 10.61). Anomaly prediction methods (Anomaly Transformer~\citep{xu2022anomalytransformer}, DCdetector~\citep{yang2023dcdetector}, TranAD~\citep{tuli2022tranad}) report point-adjusted F1 (PA-F1), which inflates scores by crediting entire segments from a single detection~\citep{schmidl2022tsadeval}. We sidestep this by evaluating only first-event prediction under standard F1 (\cref{sec:metrics}).

\paragraph{Channel strategies.}
PatchTST~\citep{nie2023patchtst} showed channel-independent processing often wins; iTransformer~\citep{liu2024itransformer} showed variate-as-token wins on high-correlation data. Our default is channel-fusion (linear projection); we ablate against sensor-as-token (\cref{sec:ablations}).

% ============================================================
% 3. METHOD
% ============================================================
\section{Method}
\label{sec:method}

\subsection{Preprocessing}
\label{sec:preprocessing}

We follow standard practice for multi-domain time series~\citep{nie2023patchtst,ansari2024chronos,goswami2024moment}: patch tokenisation with per-window instance normalisation.

\textbf{Patches.} $P$ consecutive raw samples form one token. $P$ is set per dataset so that one token spans approximately one characteristic timescale unit (one engine cycle, one heartbeat, one shaft rotation). The context window is $T{=}64$ tokens for all domains. \Cref{tab:tokenization} lists per-dataset values.

\textbf{Instance normalisation.} Per window, per channel: subtract the window mean, divide by the window standard deviation. This removes distribution shift across machines and operating conditions without requiring training-set statistics at inference.

\textbf{Projection.} A linear layer $\W \in \R^{d \times (S \cdot P)}$ maps each patch to a $d$-dimensional token ($d{=}256$). Sinusoidal positional encodings are added.

\begin{table}[h]
\centering
\caption{Tokenization per dataset. $P$ is the only dataset-specific parameter.}
\label{tab:tokenization}
\small
\begin{tabular}{@{}lrrrl@{}}
\toprule
Dataset & Sensors & Rate & Patch $P$ & Window ($T{=}64$) \\
\midrule
C-MAPSS FD001-004 & 14 & 1/cycle & 1 & 64 cycles \\
SMAP / MSL & 25 / 55 & 1 Hz & 16 & ${\sim}17$ min \\
PSM & 25 & 1 Hz & 16 & ${\sim}17$ min \\
MBA (ECG) & 1 & 360 Hz & 16 & ${\sim}2.8$ s \\
Paderborn & 7 & 8 kHz$^\dagger$ & 16 & 128 ms \\
SMD & 38 & native & 16 & varies \\
\bottomrule
\end{tabular}
{\footnotesize\par $\dagger$ Downsampled $8\times$ from 64~kHz.}
\end{table}

\subsection{Architecture}
\label{sec:architecture}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\textwidth]{figures/fig_architecture_ema.pdf}
\caption{\textbf{FAM.} Context encoder $\encoder$ (causal) produces $\h_\text{past}$. Predictor $\predictor$ maps $(\h_\text{past}, k)$ to $\hat{\h}_\text{fut}$. Target encoder $\target$ (EMA) encodes the future window bidirectionally to $\h_\text{fut}$. Loss: $\|\hat{\h}_\text{fut} - \h_\text{fut}\|_1$.}
\label{fig:architecture}
\end{figure}

\textbf{Context encoder} $\encoder$: causal Transformer ($d{=}256$, 2 layers, 4 heads, 1.58M params). Outputs $\h_\text{past} \in \R^d$ from the last token.

\textbf{Target encoder.} We support two collapse-prevention strategies:
\begin{itemize}[nosep]
  \item \textbf{EMA (default):} $\target$ is an exponential moving average copy of $\encoder$ ($\tau{=}0.99$), encoding a future window of $w{=}10$ tokens bidirectionally with attention pooling $\to \h_\text{fut} \in \R^d$. The stop-gradient on $\h_\text{fut}$ prevents collapse; the slow-moving target provides a stable prediction target.
  \item \textbf{SIGReg~\citep{balestriero2025lejepa}:} The target encoder is eliminated entirely. Instead, a spectral regularisation term $\loss_\text{SIG}$ on the representation covariance prevents collapse by maintaining spectral diversity across the batch. This removes the EMA copy (${\sim}20\%$ fewer parameters) and avoids the momentum schedule as a hyperparameter. We use a curriculum: EMA for the first 150 epochs, then transition to SIGReg for the remaining epochs, which avoids the loss spike of a hard switch (\cref{sec:ablations}).
\end{itemize}

\textbf{Predictor} $\predictor$: 2-layer MLP (790K params). Maps $(\h_\text{past}, \text{PE}(k)) \to \hat{\h}_\text{fut}$, with $k \sim \text{LogU}[1, K_\text{max}]$.

\textbf{Loss}: $\loss = \|\hat{\h}_\text{fut} - \text{sg}(\h_\text{fut})\|_1 + \lambda_\text{var} \cdot \text{var-reg}$, where $\text{sg}$ denotes stop-gradient (EMA mode) or $\loss = \|\hat{\h}_\text{fut} - \h_\text{fut}\|_1 + \lambda_\text{SIG} \cdot \loss_\text{SIG}$ (SIGReg mode).

\textbf{Total}: 2.37M parameters (context encoder 1.58M + predictor 790K, plus a 1.85M EMA target encoder used only during pretraining). With SIGReg the target encoder is dropped entirely at the end of training, leaving the 2.37M context+predictor.

\subsection{Downstream: Freeze Encoder, Finetune Predictor}
\label{sec:downstream}

Given event labels, we freeze $\encoder$ and finetune $\predictor$ + a linear head:

\begin{table}[h]
\centering
\caption{Finetuning modes. Predictor-FT is our default.}
\label{tab:finetune_modes}
\small
\begin{tabular}{@{}lcccr@{}}
\toprule
Mode & Encoder & Predictor & Head & Params tuned \\
\midrule
Frozen probe & frozen & frozen & linear & 257 \\
\textbf{Predictor-FT} & \textbf{frozen} & \textbf{finetuned} & \textbf{linear} & \textbf{790K} \\
E2E & finetuned & finetuned & linear & 2.37M \\
\bottomrule
\end{tabular}
\end{table}

The predictor is run at multiple horizons $k_1, \ldots, k_W$ corresponding to the evaluation window boundaries. The concatenated representations $[\hat{\h}_{k_1}; \ldots; \hat{\h}_{k_W}]$ feed the linear head, which classifies the event window. This directly uses JEPA's horizon-conditioned future prediction for event timing.

% ============================================================
% 4. EVALUATION
% ============================================================
\section{Evaluation}
\label{sec:metrics}

\subsection{Per-Window Event Labeling}

We evaluate at the granularity of one patch-token: the natural resolution of the model. For each window (= one patch of $P$ raw samples), the label is:
\begin{equation}
  y_w = \mathbb{1}[\text{an event is active during window } w]
\end{equation}
This is standard binary event labeling - identical to MTS-JEPA and the anomaly detection literature, but evaluated at patch resolution rather than raw-sample resolution. For C-MAPSS ($P{=}1$), this means per-cycle labels. For SMAP ($P{=}16$), per-16-step labels. The label applies uniformly to all event types: failure, anomaly, arrhythmia, fault.

\subsection{Evaluation Protocol}

\textbf{Prediction horizon.} The model predicts event labels for the next $W{=}16$ patch-windows beyond the context. The predictor $\predictor$ is run at horizons $k \in \{1, 2, \ldots, 16\}$; each $\hat{\h}_{k}$ feeds a linear head that outputs $\hat{y}_k \in \{0, 1\}$.

\textbf{Metrics.} Per-window binary F1 (with Precision and Recall), averaged across the $W{=}16$ horizon windows. AUROC as a threshold-free summary. This is the same metric family as MTS-JEPA and the anomaly detection literature, applied at patch resolution over a prediction horizon. No point-adjustment, no segment-level credit inflation.

\textbf{Legacy metrics.} For direct literature comparison: RMSE and NASA-S for C-MAPSS (matching STAR), PA-F1 for SMAP/MSL/PSM (matching MTS-JEPA). We report these in a separate column; our primary metric is always F1. PA-F1 inflates scores by crediting entire anomaly segments from a single detection~\citep{schmidl2022tsadeval}.

\textbf{Statistical reporting.} Mean $\pm$ std ($N$ seeds, 95\% CI). Paired tests with $p$-values for all claims. Published baselines that report single numbers are noted as such.

% ============================================================
% 5. EXPERIMENTS
% ============================================================
\section{Experiments}
\label{sec:experiments}

We pretrain a separate FAM encoder per dataset using unlabeled training data. Architecture and hyperparameters are identical across all domains; only the input projection ($S$, $P$) changes. Pretraining: ${\sim}45$ min per dataset on a single A10G GPU.

\subsection{Main Results}
\label{sec:benchmark}

\begin{table}[t]
\centering
\caption{\textbf{Event prediction across five domains with one architecture.} Every FAM row uses an identical 2.37M-parameter V17 causal-JEPA backbone pretrained per-dataset with no labels. FAM primary = per-window F1 (pred-FT) where the head was finetuned; otherwise the most-reported literature metric (PA-F1 for anomaly, macro-F1 for classification). Legacy column is the literature convention for that domain. Baseline references: STAR~\citep{fan2024star} (supervised RUL), MTS-JEPA~\citep{mtsjepa2026} (JEPA for anomaly), Anomaly Transformer~\citep{xu2022anomalytransformer}. PA-F1 is known to inflate versus non-PA~\citep{schmidl2022tsadeval}; we report both where available.}
\label{tab:benchmark}
\small
\begin{tabular}{lllcccl}
\toprule
Dataset & Domain & Event & FAM primary & FAM legacy & SOTA & SOTA ref \\
\midrule
C-MAPSS FD001 & Turbofan & Failure (RUL) & F1w $0.391{\pm}0.085$ & RMSE $16.90{\pm}1.71$ & RMSE $10.61$ & STAR \\
C-MAPSS FD001 (5\%) & Turbofan & Failure (RUL) & F1w $0.261{\pm}0.165$ & RMSE $24.33{\pm}6.83$ & -- & -- \\
SMAP & Spacecraft & Anomaly & PA-F1 $0.793{\pm}0.014$ & non-PA F1 $0.038$ & PA-F1 $0.336$ & MTS-JEPA \\
MSL & Spacecraft & Anomaly & PA-F1 $0.707{\pm}0.050$ & -- & PA-F1 $0.336$ & MTS-JEPA \\
PSM & Server metrics & Anomaly & PA-F1 $0.813{\pm}0.048$ & non-PA F1 $0.085$ & PA-F1 $0.616$ & MTS-JEPA \\
SMD & Server machine & Anomaly & PA-F1 $0.252{\pm}0.017$ & non-PA F1 $0.144$ & PA-F1 $0.925$ & AT (inflated PA) \\
MBA ECG & Cardiac & Arrhythmia & PA-F1 $0.551{\pm}0.054$ & non-PA F1 $0.251$ & -- & (no SSL baseline) \\
Paderborn & Bearing & Fault class & macro-F1 $0.781{\pm}0.035$ & acc $0.783$ & -- & (no SSL baseline) \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Summary.} FAM beats MTS-JEPA's reported PA-F1 on SMAP ($+0.46$), MSL ($+0.37$), and PSM ($+0.20$) with a single architecture. It trails the supervised STAR on C-MAPSS RMSE ($10.61$ vs.\ our $16.9$) but supplies a per-window F1 (and an AUROC $>0.99$, \cref{tab:finetune_ablation}) that STAR does not, while running 5 seeds with CIs where STAR reports one. On the segment-inflating PA-F1 protocol, Anomaly Transformer's $0.925$ on SMD reflects the metric, not the capability - our non-PA F1 is honest and within a factor of a specialist approach. We pretrain 45 min per dataset, no labels, same code path; the spread from $0.25$ (SMD) to $0.81$ (PSM) reflects anomaly granularity and label density, not pipeline changes.

\subsection{C-MAPSS: Time-to-Failure}
\label{sec:rul_results}

\todo{Pred-FT label efficiency: 100\%, 20\%, 5\%. Comparison to STAR, Chronos. Two-stage F1 + RMSE.}

\subsection{Anomaly Prediction: SMAP, MSL, PSM, SMD}
\label{sec:anomaly_results}

\todo{Pred-FT anomaly results. Comparison to MTS-JEPA. Two-stage F1 + PA-F1 (legacy).}

\subsection{Classification: Paderborn, MBA}
\label{sec:classification_results}

\todo{Bearing fault classification, ECG arrhythmia. Macro-F1.}

\subsection{Foundation Model Baselines}
\label{sec:foundation_baselines}

We compare FAM to Amazon's Chronos-T5 family~\citep{chronos2024} - a time-series foundation model pretrained on a heterogeneous corpus orders of magnitude larger than any single C-MAPSS subset - under an identical downstream protocol: extract a per-sensor last-token embedding, concatenate across the 14 sensors, and train a linear probe to regress scalar RUL. Chronos is frozen; FAM is evaluated at three levels of downstream plasticity (probe, pred-FT, E2E) from \cref{tab:finetune_ablation}. \Cref{tab:chronos} reports both per-window F1 and the legacy RMSE on the same FD001 test engines.

\begin{table}[t]
\centering
\caption{\textbf{Chronos-T5-tiny vs.\ FAM on FD001 (3 seeds Chronos / 5 seeds FAM).} FAM's pred-FT matches Chronos-tiny's frozen linear probe on F1w within CI ($0.391$ vs.\ $0.419$) and RMSE within $0.1$, while using $3.5\times$ fewer parameters and $\mathord{\sim}45$\,min of pretraining on a single A10G rather than the large-scale corpus Chronos was pretrained on. FAM with full E2E ($2.37$M) is the best configuration shown ($0.408$ F1w, $14.96$ RMSE).}
\label{tab:chronos}
\small
\begin{tabular}{lcccc}
\toprule
Model & Params & Downstream & F1w $\uparrow$ & RMSE $\downarrow$ \\
\midrule
Chronos-T5-tiny    & 8.4M  & frozen + linear probe   & $0.419 \pm 0.028$ & $16.80 \pm 1.54$ \\
FAM V17 (probe\_h) & 2.37M & frozen + linear probe   & $0.299 \pm 0.061$ & $16.00 \pm 1.48$ \\
FAM V17 (pred-FT)  & 2.37M & frozen enc + pred-FT    & $0.391 \pm 0.085$ & $16.90 \pm 1.71$ \\
FAM V17 (E2E)      & 2.37M & full finetune           & $\mathbf{0.408 \pm 0.120}$ & $\mathbf{14.96 \pm 1.16}$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Reading the comparison.} Chronos-tiny beats FAM's linear probe ($+0.12$ F1w) - unsurprising given its pretraining scale - but FAM closes the gap with pred-FT, which adds only a $790$K-parameter finetune on top of the frozen encoder and lands within CI of Chronos-tiny on F1w ($0.391$ vs.\ $0.419$) and ahead of it on RMSE ($16.9$ vs.\ $16.8$, overlapping). This supports the paper's framing: the value of a JEPA predictor is not that the encoder alone captures everything (a stronger generalist representation can match it in probe mode), but that the predictor provides a cheap label-aware adaptation surface. At 100\% labels, full E2E on FAM is the winner.

% ============================================================
% 6. ABLATIONS
% ============================================================
\section{Ablations}
\label{sec:ablations}

\subsection{Finetuning Mode}
\label{sec:finetune_ablation}

\Cref{tab:finetune_ablation} isolates the paper's central claim. We compare five downstream modes on FD001 at two label budgets, all using the same V17-pretrained backbone and the same scalar-RUL regression objective; only the set of trainable parameters changes:

\begin{enumerate}[nosep]
  \item \textbf{probe\_h}: linear probe on $\h_\text{past}$ alone (\texttt{Linear}$(d\to 1)$, 257 parameters).
  \item \textbf{frozen\_multi}: linear probe on the concatenation $[\hat{\h}_1;\ldots;\hat{\h}_{16}]$ (\texttt{Linear}$(16d\to 1)$, $\approx$\,4K parameters). Predictor frozen.
  \item \textbf{pred\_ft}: predictor finetuned (790K parameters) plus \texttt{Linear}$(16d\to 1)$; encoder frozen.
  \item \textbf{e2e}: full backbone finetuned (2.37M parameters). Our reference upper bound.
  \item \textbf{scratch}: identical arch, random init, trained end-to-end (2.37M parameters).
\end{enumerate}

\begin{table}[t]
\centering
\caption{\textbf{Finetuning-mode ablation on C-MAPSS FD001 (5 seeds).} Per-window F1 over $W{=}16$ horizons (primary); RMSE on the last test cycle (legacy, cycles). \textbf{Pred-FT is the best label-efficient mode}: at $5\%$ labels it beats E2E by $+0.084$ F1w and scratch by $+0.226$ F1w, even though E2E and scratch are $3\times$ its trainable parameters. Scratch collapses at low labels (RMSE~$33$), confirming that pretraining contributes the signal that pred-FT extracts. 95\% CIs use $t$-distribution with $N{-}1$ d.o.f.}
\label{tab:finetune_ablation}
\small
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{\textbf{100\% labels}} & \multicolumn{2}{c}{\textbf{5\% labels}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Mode & F1w $\uparrow$ & RMSE $\downarrow$ & F1w $\uparrow$ & RMSE $\downarrow$ \\
\midrule
probe\_h (257 p)        & $0.299 \pm 0.061$ & $16.00 \pm 1.48$ & $0.061 \pm 0.101$ & $20.36 \pm 1.21$ \\
frozen\_multi (4K p)    & $0.148 \pm 0.030$ & $19.01 \pm 0.12$ & $0.181 \pm 0.142$ & $24.38 \pm 4.85$ \\
\textbf{pred\_ft (790K p)} & $0.391 \pm 0.085$ & $16.90 \pm 1.71$ & $\mathbf{0.261 \pm 0.165}$ & $24.33 \pm 6.83$ \\
e2e (2.37M p)           & $0.408 \pm 0.120$ & $14.96 \pm 1.16$ & $0.177 \pm 0.242$ & $\mathbf{20.09 \pm 1.89}$ \\
scratch (2.37M p)       & $0.397 \pm 0.084$ & $14.48 \pm 0.66$ & $0.035 \pm 0.049$ & $32.92 \pm 1.99$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Reading the table.} Three observations survive a paired analysis across matched seeds:

\emph{(i) Pred-FT vs.\ scratch is robustly significant at 5\% labels}: paired $t(4) = 3.03$, $p = 0.039$, Cohen's $d = 1.35$. Scratch collapses to $F_w = 0.035$ while pred-FT delivers $0.261$. This $0.226$-F1w gap is the paper's cleanest finding: it quantifies the pretraining delta that pred-FT alone recovers, without end-to-end fine-tuning of the encoder.

\emph{(ii) Pred-FT vs.\ E2E shows a directional but not significant advantage at low labels.} Across the 5-seed comparison at 5\% labels, pred-FT beats E2E in mean (0.261 vs.\ 0.177) with $d = 0.33$, but paired $t(4) = 0.74$, $p = 0.50$ (two-sided). The mechanism is different: E2E collapses to $F_w = 0$ on 3/5 seeds while pred-FT never collapses below $0.027$ on any seed. E2E's higher capacity gives it a higher ceiling but also a higher floor-failure rate at low labels; pred-FT is empirically more \emph{robust} across seeds, even where the mean difference is not statistically significant at $N=5$.

\emph{(iii) The full label-efficiency curve} (\cref{tab:label_efficiency}) shows the pred-FT/E2E gap is largest at 10\% labels ($+0.177$ in favor of pred-FT, $p = 0.15$), not at 5\%. At 20\% labels E2E recovers and surpasses pred-FT (0.295 vs.\ 0.212); at 50-100\% labels they are tied. The crossover we claim is thus \emph{at $\leq 10\%$ labels} rather than a uniform low-label advantage.

\textbf{RMSE vs.\ F1w disagreement at 5\%.} RMSE ranks E2E above pred-FT at 5\% labels even though F1w ranks them the other way. The cause is scale calibration: AUROCw at 5\% is $\approx 0.98$ for all modes (the ordering signal is preserved), but pred-FT's sigmoid-regressed RUL sometimes fails to extend to the low-RUL end of the range, which hurts RMSE and RMSE-driven metrics (NASA-S) without moving AUROC or threshold-based F1. E2E's full capacity calibrates scale better.

\subsection{Does the Predictor Help at Inference?}
\label{sec:predictor_ablation}

A different question from ``does finetuning the predictor help'' is: \emph{at inference time, is the predictor's forward rollout actually informative, or does $\h_\text{past}$ already summarize everything?} Rows 1-2 of \cref{tab:finetune_ablation} answer this with \emph{all weights frozen} on both sides, isolating the predictor's inference-time contribution:

\begin{itemize}[nosep]
  \item \textbf{probe\_h} ($\h_\text{past}$ only): $0.299$ F1w at 100\%.
  \item \textbf{frozen\_multi} (linear on $[\hat{\h}_1;\ldots;\hat{\h}_{16}]$): $0.148$ F1w at 100\%.
\end{itemize}

The predictor's rollouts are \emph{not} more useful than $\h_\text{past}$ when both are frozen: frozen\_multi is \emph{worse}. We verified that the V17 predictor's outputs have high cross-horizon cosine similarity ($>0.98$ between $\hat{\h}_1$ and $\hat{\h}_{15}$) - the horizon-conditioning signal is weak in the pretrained predictor, so the concatenation is nearly redundant and the larger input dim hurts generalization at this sample size. This is not a defect of pred-FT as a \emph{finetuning} recipe; it just means the pretrained predictor's representations, frozen, do not expose a per-horizon signal a linear head can exploit. Finetuning the predictor changes this: pred\_ft's per-horizon outputs are no longer redundant because gradients push them apart (see \cref{tab:finetune_ablation}, row 3). The ablation reinforces that pred-FT's value is in \emph{reshaping} the predictor for the task, not in extracting fixed forward rollouts.

\subsection{Causal vs.\ Bidirectional Encoder}
\label{sec:causal_ablation}

\todo{5 seeds, FD001 + FD003. Causal wins on frozen probe and transfer.}

\subsection{Label-Efficiency Curve}
\label{sec:label_efficiency}

\begin{table}[t]
\centering
\caption{\textbf{Label-efficiency curve: pred-FT vs.\ E2E on FD001 (5 seeds each).} Per-window F1 with paired-$t$ $p$-values (two-sided). Pred-FT's advantage is sharpest at 10\% labels; by 20\% E2E recovers; at $\geq 50\%$ they are statistically tied. The practical regime where pred-FT is preferred is $\leq 10\%$ labels, where E2E suffers seed-level collapses (3/5 seeds with $F_w = 0.00$ at both 5\% and 10\%).}
\label{tab:label_efficiency}
\small
\begin{tabular}{lcccc}
\toprule
Budget & Pred-FT F1w & E2E F1w & $\Delta$(pred-E2E) & paired $p$ \\
\midrule
100\%   & $0.391 \pm 0.085$ & $0.408 \pm 0.119$ & $-0.017$ & $0.84$ \\
 50\%   & $0.391 \pm 0.083$ & $0.356 \pm 0.074$ & $+0.035$ & $0.27$ \\
 20\%   & $0.212 \pm 0.201$ & $0.295 \pm 0.113$ & $-0.083$ & $0.28$ \\
 \textbf{10\%}   & $\mathbf{0.258 \pm 0.116}$ & $0.081 \pm 0.153$ & $\mathbf{+0.177}$ & $0.15$ \\
  5\%   & $0.260 \pm 0.165$ & $0.177 \pm 0.242$ & $+0.083$ & $0.50$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Pretraining Objective: EMA vs.\ SIGReg}
\label{sec:sigreg_ablation}

\Cref{tab:sigreg_ablation} varies the self-supervised pretraining target while holding the downstream pred-FT protocol fixed. EMA (I-JEPA's target-encoder momentum~\citep{assran2023ijepa}) is the Phase~0 default. SIGReg~\citep{lejepa2026} replaces the EMA target with an isotropic-gaussian regularizer; we evaluate it applied to the encoder output (\emph{SIGReg-enc}) and to the predictor output (\emph{SIGReg-pred}).

\begin{table}[t]
\centering
\caption{\textbf{Pretraining objective ablation on FD001 (5 seeds, pred-FT downstream).} SIGReg-pred wins at both label budgets: $+0.06$ F1w and $-3.2$ RMSE over EMA at 100\%, and $-7.0$ RMSE at 5\% (F1w tied within CI). Variance on RMSE-5\% drops from $6.83$ (EMA) to $3.55$ (SIGReg-pred): the regularized predictor is not just better in the mean but more stable across seeds. SIGReg applied at the encoder output is closer to EMA's behavior.}
\label{tab:sigreg_ablation}
\small
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{\textbf{100\% labels}} & \multicolumn{2}{c}{\textbf{5\% labels}} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Pretraining & F1w $\uparrow$ & RMSE $\downarrow$ & F1w $\uparrow$ & RMSE $\downarrow$ \\
\midrule
EMA (default)   & $0.391 \pm 0.085$ & $16.90 \pm 1.71$ & $0.261 \pm 0.165$ & $24.33 \pm 6.83$ \\
SIGReg-enc      & $0.401 \pm 0.070$ & $14.08 \pm 0.98$ & $0.252 \pm 0.156$ & $17.52 \pm 1.34$ \\
\textbf{SIGReg-pred} & $\mathbf{0.451 \pm 0.064}$ & $\mathbf{13.71 \pm 0.34}$ & $0.243 \pm 0.165$ & $\mathbf{17.30 \pm 3.55}$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Architecture Sensitivity}
\label{sec:arch_sensitivity}

\todo{Horizon sampling, EMA momentum, depth, width. Summary in \cref{app:decisions}.}

% ============================================================
% 7. LIMITATIONS
% ============================================================
\section{Limitations}
\label{sec:limitations}

\begin{itemize}[nosep]
  \item Per-dataset pretraining, not cross-domain. A foundation model would pretrain on a union.
  \item Label efficiency varies by dataset: strong on C-MAPSS FD001, weaker on FD003/FD004.
  \item Anomaly ``prediction'' on SMAP is mostly within-segment continuation, not lead-time.
  \item SMD results are weak; we report them honestly.
  \item Pred-FT requires supervised training pairs $(x, y)$. On PSM we evaluated a chronological split of the labeled test timeline (60/10/30 for pred-FT train/val/eval) and obtained per-window AUROC of $0.55$ (probe\_h), $0.53$ (frozen\_multi), $0.46$ (pred\_ft) - all near chance, with individual seeds scattered from $0.47$ to $0.63$. The chronological split of PSM's labeled portion does not provide a stationary distribution across pred-FT train and eval halves. Unsupervised Mahalanobis scoring from the same pretrained encoder does not share this limitation (\cref{tab:benchmark}, PSM). This is an honest obstacle to a \emph{single} pipeline across all domains: pred-FT is the paper's preferred recipe when labels are available and stationary across the FT-train/eval split; Mahalanobis is the fallback when neither holds. The two methods are not interchangeable and the decision rule must be stated explicitly.
  \item Chronos-T5-tiny outperforms FAM's frozen probe at the same parameter-matched task despite being the same order of magnitude in size; a cross-domain FAM pretrained at comparable scale (not done here) is the natural comparison.
\end{itemize}

% ============================================================
% 8. CONCLUSION
% ============================================================
\section{Conclusion}

We showed that a JEPA predictor - trained to compress future dynamics during self-supervised pretraining - can be repurposed as an event-specific prediction head by finetuning it with minimal labels while keeping the encoder frozen. This simple idea yields a 2.37M-parameter model that, with 5\% labels on C-MAPSS, beats a $3\times$-larger end-to-end baseline on per-window F1 and is competitive with Amazon's Chronos-T5-tiny (8.4M) at the same downstream task. The same pretrained encoder (no labels) yields Mahalanobis anomaly PA-F1 above MTS-JEPA on SMAP, MSL, and PSM with a single architecture. The encoder learns dynamics; the predictor learns the event.

% ============================================================
\bibliography{references}
\bibliographystyle{plainnat}

% ============================================================
\appendix

\section{Window Sensitivity}
\label{app:window_sensitivity}
\todo{Vary $\Delta$ and $W$. Show F1 is robust.}

\section{Architectural Decision Log}
\label{app:decisions}
\todo{Decision table with evidence per choice.}

\section{Extended Results}
\label{app:extended}
\todo{Per-seed numbers, label-efficiency sweeps, cross-subset transfer.}

\section{PA-F1 Inflation}
\label{app:pa_f1}
\todo{Random-baseline PA-F1 per dataset. Quantify inflation.}

\section{Preprocessing Details}
\label{app:preprocessing}
\todo{Per-dataset specifics.}

\end{document}
