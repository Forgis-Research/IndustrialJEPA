\documentclass[border=10pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc,backgrounds}

\input{colors.tex}

\begin{document}
\begin{tikzpicture}[
    >=Stealth,
    font=\sffamily\small,
    dombox/.style={
        rounded corners=3pt,
        draw=#1!70!black, line width=0.6pt, fill=#1,
        minimum height=1.6cm, minimum width=2.8cm,
        align=center, text=ctextdark
    },
    evbox/.style={
        rounded corners=2pt,
        draw=#1!60!black, fill=#1,
        minimum height=0.45cm, minimum width=2.2cm,
        align=center, font=\sffamily\scriptsize, text=ctextdark
    },
    dataarrow/.style={->,line width=0.5pt,ctextdark},
]

% ============================================================
% CENTER: Trajectory JEPA
% ============================================================
\node[rounded corners=3pt, draw=cprimary!70!black, fill=cprimarybg,
    line width=0.8pt,
    minimum height=1.6cm, minimum width=3.0cm,
    align=center, text=ctextdark] (jepa) {
    \textbf{Trajectory JEPA}\\[2pt]
    {\scriptsize One encoder}\\[-1pt]
    {\scriptsize One pretraining objective}\\[-1pt]
    {\scriptsize Trivial task probes}
};

% ============================================================
% DOMAINS (2x2 around center)
% ============================================================

% Top-left: Industrial
\node[dombox=csecondarybg, above left=0.8cm and 0.5cm of jepa] (industrial) {
    \textbf{\textcolor{csecondary}{Industrial}}\\[2pt]
    {\scriptsize C-MAPSS (turbofan)}\\[-1pt]
    {\scriptsize FEMTO (bearing)}\\[-1pt]
    {\scriptsize SWaT (water plant)}
};

% Top-right: Spacecraft
\node[dombox=caccentbg, above right=0.8cm and 0.5cm of jepa] (space) {
    \textbf{\textcolor{caccent}{Spacecraft}}\\[2pt]
    {\scriptsize SMAP (satellite)}\\[-1pt]
    {\scriptsize MSL (Mars rover)}\\[-1pt]
    {\scriptsize PSM (servers)}
};

% Bottom-left: Financial
\node[dombox=cprimarybg, below left=0.8cm and 0.5cm of jepa] (finance) {
    \textbf{\textcolor{cprimary}{Financial}}\\[2pt]
    {\scriptsize VIX (volatility)}\\[-1pt]
    {\scriptsize S\&P 500 (equities)}\\[-1pt]
    {\scriptsize Crypto (BTC/ETH)}
};

% Bottom-right: Medical
\node[dombox=chighlightbg, below right=0.8cm and 0.5cm of jepa] (medical) {
    \textbf{\textcolor{chighlight}{Medical}}\\[2pt]
    {\scriptsize PhysioNet (sepsis)}\\[-1pt]
    {\scriptsize CHB-MIT (seizure)}\\[-1pt]
    {\scriptsize Battery (SOH)}
};

% Arrows from JEPA to domains
\draw[dataarrow,csecondary] (jepa.north west) -- (industrial.south east);
\draw[dataarrow,caccent] (jepa.north east) -- (space.south west);
\draw[dataarrow,cprimary] (jepa.south west) -- (finance.north east);
\draw[dataarrow,chighlight] (jepa.south east) -- (medical.north west);

% ============================================================
% EVENT TYPES (right column)
% ============================================================

\coordinate (evcol) at ([xshift=5.0cm]jepa.east);

\node[font=\sffamily\scriptsize\bfseries, text=ctextmid,
    anchor=south] at ([yshift=1.8cm]evcol) {GREY SWAN EVENTS};

\node[evbox=csecondarybg, anchor=center] (e_rul) at ([yshift=1.2cm]evcol) {
    \textbf{Time-to-Failure} (RUL)
};
\node[font=\sffamily\scriptsize, text=ctextmid,
    right=0.15cm of e_rul] {nRMSE $\downarrow$};

\node[evbox=caccentbg, below=0.3cm of e_rul] (e_anom) {
    \textbf{Anomaly Onset}
};
\node[font=\sffamily\scriptsize, text=ctextmid,
    right=0.15cm of e_anom] {F1 $\uparrow$};

\node[evbox=cprimarybg, below=0.3cm of e_anom] (e_tte) {
    \textbf{Threshold Exceedance}
};
\node[font=\sffamily\scriptsize, text=ctextmid,
    right=0.15cm of e_tte] {nRMSE $\downarrow$};

\node[evbox=chighlightbg, below=0.3cm of e_tte] (e_var) {
    \textbf{VaR Breach} (financial)
};
\node[font=\sffamily\scriptsize, text=ctextmid,
    right=0.15cm of e_var] {nRMSE $\downarrow$};

% ============================================================
% DOTTED CONNECTIONS (domains → event types)
% Route around boxes via waypoints
% ============================================================

% Industrial → RUL, TTE
\coordinate (ind_exit) at (industrial.east |- e_rul.west);
\draw[dotted, csecondary!50, line width=0.4pt]
    (industrial.east) -- ++(0.3,0) |- (e_rul.west);
\draw[dotted, csecondary!50, line width=0.4pt]
    (industrial.east) -- ++(0.3,0) |- (e_tte.west);

% Spacecraft → Anomaly
\draw[dotted, caccent!50, line width=0.4pt]
    (space.east) -- ++(0.2,0) |- (e_anom.west);

% Financial → TTE, VaR
\draw[dotted, cprimary!50, line width=0.4pt]
    (finance.east) -- ++(0.3,0) |- (e_tte.west);
\draw[dotted, cprimary!50, line width=0.4pt]
    (finance.east) -- ++(0.3,0) |- (e_var.west);

% Medical → RUL, TTE
\draw[dotted, chighlight!50, line width=0.4pt]
    (medical.east) -- ++(0.2,0) |- (e_rul.west);
\draw[dotted, chighlight!50, line width=0.4pt]
    (medical.east) -- ++(0.2,0) |- (e_tte.west);

% ============================================================
% BOTTOM TAGLINE
% ============================================================
\node[font=\sffamily\scriptsize, text=ctextmid, align=center,
    below=0.4cm of $(finance.south)!0.5!(medical.south)$] {
    \textbf{Key:} Every baseline fills $\leq$2 columns.
    Trajectory JEPA fills all four.
};

\end{tikzpicture}
\end{document}
