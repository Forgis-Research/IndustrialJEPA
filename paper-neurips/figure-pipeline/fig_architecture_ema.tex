\documentclass[border=12pt]{standalone}
\usepackage{tikz}
\usepackage{amsmath,amssymb}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc,
    decorations.pathmorphing,decorations.pathreplacing,backgrounds}

\input{colors.tex}

\begin{document}
\begin{tikzpicture}[
    >=Stealth,
    font=\sffamily\small,
    dataarrow/.style={->,line width=0.8pt,ctextdark},
    softarrow/.style={->,line width=0.6pt,ctextmid},
    sechead/.style={font=\sffamily\scriptsize\bfseries,text=ctextmid},
    annot/.style={font=\sffamily\scriptsize,text=ctextmid},
    compblock/.style={
        rounded corners=3pt, line width=0.5pt,
        minimum height=1.4cm, minimum width=2.5cm,
        align=center, text=ctextdark, inner sep=5pt,
    },
    capsule/.style={
        rounded corners=8pt, draw=cborder, fill=white,
        line width=0.5pt, minimum height=1.5cm, minimum width=0.55cm,
        inner sep=2pt,
    },
    probe/.style={
        rounded corners=10pt, draw=cborder, fill=cbgwarm,
        line width=0.5pt, minimum height=0.55cm, minimum width=2.0cm,
        align=center, text=ctextdark, inner sep=3pt,
    },
]

% ============================================================
% PHASE LABELS (top)
% ============================================================

% Left phase: Self-Supervised Pretraining
\node[sechead] at (4.5,4.2) {SELF-SUPERVISED PRETRAINING (no event labels)};

% Right phase: Supervised Fine-tuning
\node[sechead] at (13.8,4.2) {SUPERVISED PROBES};

% Vertical separator
\draw[cborder,line width=0.5pt,dashed]
    (11.0,4.5) -- (11.0,-3.8);

% ============================================================
% LEFT HALF: SSL Pretraining
% ============================================================

% --- Section headers ---
\node[annot] at (0,3.4) {input};
\node[annot] at (3.3,3.4) {encoders};
\node[annot] at (6.6,3.4) {latent};
\node[annot] at (9.2,3.4) {prediction};

% --- Past input ---
\node[rounded corners=2pt, draw=cborder, fill=white,
    minimum height=1.8cm, minimum width=1.4cm,
    inner sep=0pt] (past) at (0,1.8) {};
\begin{scope}
    \clip ([xshift=1pt,yshift=1pt]past.south west)
        rectangle ([xshift=-1pt,yshift=-1pt]past.north east);
    \foreach \yoff/\col in {0.5/csecondary,0.25/csecondary!75,
        0.0/csecondary!55,-0.25/csecondary!75,-0.5/csecondary!40} {
        \draw[\col,line width=0.4pt,
              decorate,decoration={snake,amplitude=1pt,segment length=5pt}]
            ([xshift=4pt,yshift=\yoff cm]past.center)
            -- ([xshift=-4pt,yshift=\yoff cm]past.center);
    }
\end{scope}
\node[font=\sffamily\scriptsize,csecondary,below=0.2cm of past] {$\mathbf{x}_{1:t}$};

% --- Future input ---
\node[rounded corners=2pt, draw=cborder, fill=white,
    minimum height=1.8cm, minimum width=1.4cm,
    inner sep=0pt, below=1.2cm of past] (future) {};
\begin{scope}
    \clip ([xshift=1pt,yshift=1pt]future.south west)
        rectangle ([xshift=-1pt,yshift=-1pt]future.north east);
    \foreach \yoff/\col in {0.5/caccent,0.25/caccent!75,
        0.0/caccent!55,-0.25/caccent!75,-0.5/caccent!40} {
        \draw[\col,line width=0.4pt,
              decorate,decoration={snake,amplitude=1pt,segment length=5pt}]
            ([xshift=4pt,yshift=\yoff cm]future.center)
            -- ([xshift=-4pt,yshift=\yoff cm]future.center);
    }
\end{scope}
\node[font=\sffamily\scriptsize,caccent,below=0.2cm of future] {$\mathbf{x}_{t+1:t+k}$};

% --- Context Encoder ---
\node[compblock, draw=csecondary!40, fill=csecondarybg,
    right=1.2cm of past] (enc_ctx) {
    {\scriptsize\textbf{Context Encoder}}\\[2pt]
    $f_\theta$\\[1pt]
    {\scriptsize Causal Transformer}
};
\draw[dataarrow] (past.east) -- (enc_ctx.west);

% --- Target Encoder ---
\node[compblock, draw=caccent!40, fill=caccentbg,
    right=1.2cm of future] (enc_tgt) {
    {\scriptsize\textbf{Target Encoder}}\\[2pt]
    $\bar{f}_\xi$ {\scriptsize(EMA)}\\[1pt]
    {\scriptsize Bidirectional + Pool}
};
\draw[dataarrow] (future.east) -- (enc_tgt.west);

% --- EMA connection ---
\draw[->,line width=0.8pt,caccent!70,dashed]
    ([xshift=0.5cm]enc_ctx.south) -- ([xshift=0.5cm]enc_tgt.north)
    node[midway,right=0.2cm,annot,align=left] {
        EMA\\[-1pt]
        {\scriptsize$\xi\!\leftarrow\!\tau\xi\!+\!(1\!-\!\tau)\theta$}
    };

% --- h_past capsule ---
\node[capsule,right=0.8cm of enc_ctx] (h_past) {};
\foreach \y in {0.4,0.2,0.0,-0.2,-0.4} {
    \fill[csecondary!55] ([yshift=\y cm]h_past.center) circle (1.3pt);
}
\node[annot,above=0.2cm of h_past] {$\mathbf{h}_\text{past}$};
\draw[dataarrow] (enc_ctx.east) -- (h_past.west);

% --- h_fut capsule ---
\node[capsule,right=0.8cm of enc_tgt] (h_fut) {};
\foreach \y in {0.4,0.2,0.0,-0.2,-0.4} {
    \fill[caccent!55] ([yshift=\y cm]h_fut.center) circle (1.3pt);
}
\node[annot,above=0.2cm of h_fut] {$\mathbf{h}_\text{fut}$};
\node[annot,below=0.2cm of h_fut] {sg[$\cdot$]};
\draw[dataarrow] (enc_tgt.east) -- (h_fut.west);

% --- Predictor ---
\node[compblock, draw=cprimary!40, fill=cprimarybg,
    right=1.0cm of h_past, minimum width=2.2cm] (predictor) {
    {\scriptsize\textbf{Predictor}}\\[2pt]
    $g_\phi$\\[1pt]
    {\scriptsize Horizon MLP}
};
\draw[dataarrow] (h_past.east) -- (predictor.west);

% --- Horizon k ---
\node[diamond, draw=cprimary!40, fill=cprimarybg,
    line width=0.5pt, inner sep=2pt,
    above=0.5cm of predictor] (horizon) {{\scriptsize $k$}};
\node[annot,left=0.2cm of horizon] {$k\!\sim\!\mathcal{U}[5,30]$};
\draw[softarrow] (horizon.south) -- (predictor.north);

% --- Predicted h ---
\node[capsule,right=0.5cm of predictor] (h_hat) {};
\foreach \y in {0.4,0.2,0.0,-0.2,-0.4} {
    \fill[cprimary!45] ([yshift=\y cm]h_hat.center) circle (1.3pt);
}
\node[annot,above=0.2cm of h_hat] {$\hat{\mathbf{h}}_\text{fut}$};
\draw[dataarrow] (predictor.east) -- (h_hat.west);

% --- L1 Loss ---
\node[compblock, draw=chighlight!40, fill=chighlightbg,
    minimum width=1.5cm, minimum height=1.0cm]
    (loss) at ($(h_hat.south)!0.5!(h_fut.north)$) {
    {\scriptsize\textbf{$\mathcal{L}_1$ Loss}}\\[1pt]
    {\scriptsize $\|\hat{\mathbf{h}}\!-\!\text{sg}[\mathbf{h}]\|_1$}
};
\draw[dataarrow] (h_hat.south) -- (loss.north);
\draw[dataarrow] (h_fut.north) -- (loss.south);

% ============================================================
% RIGHT HALF: Supervised Probes (downstream fine-tuning)
% ============================================================

% Arrow from h_past across the separator to probes
\coordinate (probe_entry) at (12.0,1.8);
\draw[dataarrow,line width=1.0pt,csecondary]
    (h_past.east) -- ++(0.15,0)
    .. controls ++(0.5,0) and ++(-1.5,0) .. (probe_entry)
    node[pos=0.5,above=0.15cm,annot] {frozen or fine-tuned};

% Anomaly arrow from loss
\coordinate (anom_entry) at (12.0,-1.0);
\draw[dataarrow,line width=0.8pt,chighlight,dashed]
    (loss.east) -- ++(0.3,0) |- (anom_entry);

% --- Probe boxes (stacked vertically on the right) ---
\node[probe,anchor=west] (p_rul) at (12.3,2.5) {
    {\scriptsize \textbf{RUL}~~$\mathbf{h}\!\to\!\hat{y}$}
};
\node[annot,right=0.2cm of p_rul] {linear, supervised};

\node[probe,anchor=west] (p_tte) at (12.3,1.6) {
    {\scriptsize \textbf{TTE}~~$\mathbf{h}\!\to\!\hat{\tau}$}
};
\node[annot,right=0.2cm of p_tte] {linear, supervised};

\node[probe,anchor=west] (p_fault) at (12.3,0.7) {
    {\scriptsize \textbf{Fault}~~$\mathbf{h}\!\to\!K$}
};
\node[annot,right=0.2cm of p_fault] {linear, supervised};

\node[probe,anchor=west,fill=chighlightbg,draw=chighlight!30]
    (p_anom) at (12.3,-0.5) {
    {\scriptsize \textbf{Anomaly}~~$\|\hat{\mathbf{h}}\!-\!\mathbf{h}\|_1$}
};
\node[annot,text=cprimary,right=0.2cm of p_anom] {unsupervised};

% Dots to suggest extensibility
\node[annot] at (13.3,-1.3) {$\vdots$};
\node[annot,align=left] at (13.3,-1.9) {
    {\scriptsize any event type}\\[-1pt]
    {\scriptsize via linear probe}
};

% Connect entry point to probes
\draw[softarrow] (probe_entry) -- ++(0.1,0) |- (p_rul.west);
\draw[softarrow] (probe_entry) -- (p_tte.west);
\draw[softarrow] (probe_entry) -- ++(0.1,0) |- (p_fault.west);
\draw[softarrow,dashed,chighlight] (anom_entry) -- ++(0.1,0) |- (p_anom.west);

% ============================================================
% BOTTOM: Brief annotation
% ============================================================

\node[annot,anchor=west] at (-0.8,-3.5) {
    \textbf{Left:} pretraining uses \emph{no event labels}.
    \textbf{Right:} trivial probes adapt to any grey swan task.
    Anomaly detection is free (prediction error $=$ anomaly score).
};

\end{tikzpicture}
\end{document}
